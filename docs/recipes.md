# Recipes

Common usage patterns and examples.

## Code Review

```bash
kairos -p "Review src/main.ts for security issues"
```

## Refactoring

```bash
kairos -p "Refactor src/utils.ts to use functional patterns"
```

## Test Generation

```bash
kairos -p "Generate tests for src/llm/client.ts"
```

## Documentation

```bash
kairos -p "Generate API documentation for src/llm/"
```

## Debugging

```bash
kairos -p "Help me debug the error in tests/local-models.test.ts"
```

## Git Operations

```bash
kairos -p "Create a commit with message 'Fix streaming timeout'"
```

## Memory Management

```bash
kairos -p "Remember that I prefer TypeScript strict mode"
/recall TypeScript preferences
```

## Multi-file Operations

```bash
kairos -p "Find all TODO comments in src/ and create a summary"
```

## Performance Analysis

```bash
kairos -p "Analyze bundle size and suggest optimizations"
```

## Security Audit

```bash
kairos -p "Scan for hardcoded secrets in the codebase"
```
